import React, { useContext } from "react";
import { AppContext } from "../context/context";
import Home from "./Home";

const page = () => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default page;
